# 代码审查修复任务

你是 TradingJournalAnalyzer 项目的修复工程师。根据代码审查报告修复所有发现的问题。
项目根目录：/Users/lyc/Dev/Projects/TradingJournalAnalyzer
后端目录：backend/

必读上下文文件（修复前先读）：
- CLAUDE.md — 项目设计决策与不可随意改动的金融定义
- docs/superpowers/FINANCE_DOMAIN.md — 指标权威定义
- docs/superpowers/VERIFICATION_CHECKLIST.md — 验证清单
- PROJECT_EXPERIENCE.md — 已知踩坑史
- docs/review/代码审查报告_2026-06-20.md — 完整审查报告（本次修复依据）

修复原则：
1. 每个修复只改必要代码，不做驱动式重构
2. 匹配项目现有风格（FastAPI + SQLAlchemy + Pydantic）
3. 修复后必须运行 pytest 确认不引入回归
4. 不要提交 git commit，由用户决定

## 必须修复的问题清单（按优先级）

### 🔴 阻塞项（全部修复）

**🔴-1 JWT 密钥硬编码默认值**
文件: backend/app/config.py:7
现状: secret_key: str = "change-me-in-production"
修复: 在 Settings 类初始化后（或 main.py 启动时）加校验，生产环境若 secret_key 仍为默认值或长度<32 则抛 RuntimeError 拒绝启动。保留默认值供开发用，但加 validator 警告。

**🔴-2 admin JWT 未真正隔离**
文件: backend/app/api/admin.py, backend/app/auth/jwt.py
现状: admin_login 用与普通登录相同的 create_token，_require_admin 复用 get_current_user 仅靠 DB is_admin 区分
修复: 在 JWT payload 中加 "scope":"admin" claim 用于 admin token；admin_login 签发时带此 claim；_require_admin 校验该 claim 存在。修改 jwt.py 的 create_token 增加可选 scope 参数。

**🔴-3 report.py 的 _load_trades 未 scope 到 raw_file_id 且未过滤软删除**
文件: backend/app/api/report.py:45-58
现状: 按 user_id + 日期范围加载，是 analysis.py 已修复版本的旧副本
修复: 删除 report.py 的 _load_trades，改为从 analysis.py 导入复用（或抽到公共模块）。确保 AI 报告与 stats 端点使用同一份数据边界，含 is_deleted==False 过滤和 raw_file_id scope。

**🔴-4 smart.py 循环中途翻转 is_future**
文件: backend/app/parsers/smart.py:332-333
现状: 基于逐行 qty<100 翻转全局 is_future 标志
修复: 删除 line 332-333 的中途翻转。资产类型是文件级属性，已在 line 282 由列级分类确定，循环内不再变异。

**🔴-5 方向判断子串匹配"开"误判**
文件: backend/app/parsers/smart.py:322-327
现状: "开" in value 同时匹配"买入开仓"和"卖出开仓"
修复: 优先用精确集合匹配（raw_dir in _DIRECTION_BUY），子串匹配仅作回退且需更精确（如"买入"而非单字"买"）。无法识别时不要默认 BUY，改为跳过该行并记录。保持对 A 股现货的兼容性。

**🔴-6 部分孤儿卖出被静默丢弃**
文件: backend/app/engine/position.py:116-136
现状: total_qty>0 and remaining>0 时孤儿部分被丢弃
修复: 在 line 136 的 `if total_qty > 0:` 块之后，增加 `if total_qty > 0 and remaining > 0:` 分支，为剩余 remaining 股生成 cost_known=False 的 orphan position（参考 line 116-134 纯孤儿的处理逻辑）。

**🔴-7 部分匹配时 sell_comm 未按比例分摊**
文件: backend/app/engine/position.py:139-140
现状: sell_comm = trade_comm（全量），但 total_buy_comm 已按比例分摊
修复: 改为 sell_comm = trade_comm * (total_qty / trade.quantity) if trade.quantity > 0 else 0

**🔴-8 AVERAGE_DOWN 用最终 PnL 判断加仓时亏损**
文件: backend/app/engine/pattern.py:199-219
现状: 用 prev_pos.pnl < 0（最终盈亏）判断加仓时亏损状态
修复: 改用加仓时刻的浮亏判断。由于 market_data 在此函数不一定可用，用价格推断：last_buy_price < prev_pos.avg_entry_price 表示加仓时价格低于前序成本（即处于浮亏）。将 `prev_pos.pnl < 0` 改为 `last_buy_price < prev_pos.avg_entry_price`。fallback 路径（line 238-248）本来已用价格比较，无需改，但要确认逻辑一致。

**🔴-9 文件上传无大小/类型限制**
文件: backend/app/api/upload.py:33
现状: content = file.file.read() 无大小上限
修复: 增加大小限制（MAX_UPLOAD_BYTES = 10*1024*1024 即 10MB），读取前检查 Content-Length 或读取时累计字节超限返回 413。增加扩展名白名单校验（.csv/.xls/.xlsx，大小写不敏感）。

**🔴-10 全局无速率限制**
文件: backend/app/api/auth.py, admin.py, main.py
修复: 引入 slowapi 限流。对 /api/auth/login、/api/admin/login 限 5次/分钟/IP。若 slowapi 未安装，用 pip install slowapi 安装到 .venv。在 main.py 注册 limiter 中间件，在对应路由加 @limiter.limit("5/minute") 装饰器。注意 slowapi 需要在 request 中获取 IP。

### 🟡 建议项（全部修复）

**🟡-1 AI provider 无超时无重试**
文件: backend/app/ai/provider.py:33-86, validator.py:120-134
修复: 为每个 async client 设 timeout=60；generate_with_retry 用 try/except 包裹 provider.generate，对超时/429/5xx 重试；响应解析前检查 choices/content 非空。

**🟡-2 store_bars 逐条查重 N+1**
文件: backend/app/engine/market_data.py:74-80
修复: 改为批量。先一次性查出已存在的 (symbol,date) 集合，内存过滤后批量 add。或用 INSERT ON CONFLICT。

**🟡-3 gross_loss 口径不一致**
文件: backend/app/engine/insight.py:136, 206
修复: 将 p.pnl < 0 改为 p.pnl <= 0，与 analysis.py 和 FINANCE_DOMAIN.md 一致（平本交易计入亏损分母）。

**🟡-4 stats_snapshot 首次写入后永不更新**
文件: backend/app/api/analysis.py:288-301
修复: 当 raw_file_id 变更或交易被软删除时清空 stats_snapshot 使其重新计算。简单做法：在 clear_trades 或 re-upload 时置空 stats_snapshot。

**🟡-5 report.py 生成报告未传 market_data**
文件: backend/app/api/report.py:116-118
修复: report 生成路径调用 tag_market_patterns（需 market_data），或复用 analysis.py 的 _build_category_map 确保标签完整。需先获取 market_data（参考 analysis.py 的做法）。

**🟡-6 PYRAMID/AVERAGE_DOWN first_buy_price 跨持仓周期**
文件: backend/app/engine/pattern.py:176-177
修复: first_buy_price 应取当前持仓周期的首次买入价，而非历史首笔。找到 last_buy 之前最近一次"清仓"之后的首次买入价。可用 all_positions 中 prev_pos 的 entry_price。

**🟡-7 CHASE 只满足涨幅也打标签**
文件: backend/app/engine/pattern.py:419-429
修复: 严格按 FINANCE_DOMAIN.md 三条件 AND：5日涨幅>15% AND 入场价>MA20×1.10 AND 入场价≥20日高点×0.97，都满足才打 CHASE。不满足全部条件的不再打标签（不要打低置信度版本）。

**🟡-8 detect/parse 静默吞异常无日志**
文件: backend/app/parsers/smart.py:237-238, 312-368
修复: detect 的 except 加 logging.exception；parse 的 except: continue 累计跳过行数，若跳过比例超 5% 在返回结果或日志中标记 warning。

**🟡-9 run_analysis 未校验 raw_file 归属**
文件: backend/app/api/analysis.py:72-87
修复: 创建 Analysis 前，若 body.raw_file_id 存在，校验 RawFile.user_id == current_user.id，否则 404。

**🟡-10 JWT 7天长效无吊销**
文件: backend/app/config.py:9, backend/app/auth/jwt.py
修复: 缩短 access token 到 30 分钟（jwt_expire_minutes=30），payload 加 iat。注：完整 refresh token 机制改动较大，本次先缩短有效期 + 加 iat 即可。

**🟡-11 下载文件名 CRLF 过滤不完整**
文件: backend/app/api/admin.py:14-16
修复: _safe_filename 改为白名单 re.sub(r'[^A-Za-z0-9._-]', '_', name)，并使用 RFC 5987 编码 filename*=UTF-8''<percent-encoded>。

**🟡-12 扩展名检查大小写敏感**
文件: backend/app/parsers/base.py:117-132
修复: filename.lower().endswith(...)。

**🟡-13 并发锁仅进程内**
文件: backend/app/engine/market_fetcher.py:25-26
修复: 在 store_bars 层用 DB 唯一约束 (symbol, date) + ON CONFLICT DO NOTHING 保证幂等（这同时解决了 🟡-2）。进程内锁保留用于减少重复网络拉取。若 DailyBar 表无唯一约束，需加 __table_args__ = (UniqueConstraint('symbol','date'),)。

**🟡-14 Validator 用全文第一个数字校验**
文件: backend/app/ai/validator.py:42-46
修复: 用带语义上下文的正则，如 r"交易次数[：:\s]*(\d+)"，r"胜率[：:\s]*(\d+(?:\.\d+)?)%"。若匹配不到则跳过该校验（不报错）。

**🟡-15 mootdx 客户端无重连**
文件: backend/app/engine/market_fetcher.py:29-36
修复: 捕获连接异常后置 _CLIENT=None 触发下次重连。

### 💭 小改进（全部修复）

**💭-1** insight.py:20 移除未使用的 pattern_name 参数（注意检查所有调用点同步修改）
**💭-2** mae.py:106 profit_capture_ratio 加 mfe_pct > 0.001 下限过滤
**💭-3** market_fetcher.py:86,128 异常吞没处加 logger.warning
**💭-4** analysis.py:304 stats 端点响应加 is_small_sample: bool 字段（valid_count < 5 时为 True）
**💭-5** analysis.py:160 函数内 import RawFile 提升为模块级
**💭-6** analysis.py:122 Trade.is_deleted == False 改为 .is_(False)
**💭-7** analysis.py 与 report.py 的 _load_analysis 重复定义抽到公共模块（与 🔴-3 一起做）
**💭-8** schemas/auth.py:8 email 改用 EmailStr（需 from pydantic import EmailStr，注意 email-validator 依赖）
**💭-9** auth.py:61 注册返回 409 改为通用提示"注册失败，请检查输入"避免账户枚举
**💭-10** main.py:15 lifespan 的 create_all 保留（迁移到 Alembic 改动太大），但加注释说明生产应用 Alembic

### 测试失败修复

tests/test_engine/test_pattern.py 有 28 个失败，根因是测试期望旧标签名 TREND/COUNTER_TREND，但代码已改为 BULL_TREND/BEAR_TREND。
修复: 更新测试中的期望值，TREND→BULL_TREND（或根据上下文 BEAR_TREND），COUNTER_TREND 移除或对应调整。运行 pytest 确认全部通过。

## 执行步骤

1. 先读所有相关文件理解现状
2. 按优先级从 🔴-1 到 🔴-10 依次修复
3. 修复 🟡-1 到 🟡-15
4. 修复 💭-1 到 💭-10
5. 修复测试失败
6. 运行 cd backend && source .venv/bin/activate && python -m pytest tests/ -q 确认全部通过
7. 报告每个问题的修复状态（已修复/部分修复/无法修复+原因）

注意：
- pip install 任何新依赖都装到 backend/.venv
- 修改后立即运行相关测试验证
- 不要 git commit
- 输出最终修复总结
